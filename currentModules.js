export const returnArgs = (allArgs = process.argv) => allArgs.slice(2)
export const returnArgs2 = (allArgs = process.argv) => allArgs.slice(2)
// needs "type": "module" in package.json

export default returnArgs